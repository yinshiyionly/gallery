# =============================================================================
# 测试环境配置
# =============================================================================
# 此文件用于运行测试时的环境配置

# 应用基础配置
NODE_ENV=test
PORT=3001
NEXT_PUBLIC_APP_URL=http://localhost:3001

# 测试环境专用设置
DEBUG=false
SHOW_ERROR_DETAILS=true
LOG_LEVEL=warn

# 数据库配置 (测试环境使用独立的测试数据库)
MONGODB_DB_NAME=gallery_test

# 禁用缓存以确保测试的一致性
IMAGE_CACHE_TTL=0
API_CACHE_TTL=0

# 安全配置
CORS_ORIGIN=http://localhost:3001
RATE_LIMIT_MAX=10000
RATE_LIMIT_WINDOW=60000

# NextAuth 配置
NEXTAUTH_URL=http://localhost:3001
NEXTAUTH_SECRET=test-nextauth-secret-key-must-be-at-least-32-characters-long

# JWT 配置
JWT_SECRET=test-jwt-secret-key-must-be-at-least-32-characters-long